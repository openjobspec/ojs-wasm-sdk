{
  "name": "@openjobspec/wasm",
  "version": "0.5.0",
  "description": "OJS (Open Job Spec) client SDK compiled to WebAssembly — works in browsers, Service Workers, and edge runtimes",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/openjobspec/ojs-wasm-sdk.git"
  },
  "homepage": "https://openjobspec.org",
  "keywords": [
    "jobs",
    "queue",
    "background-jobs",
    "wasm",
    "webassembly",
    "ojs",
    "open-job-spec",
    "cloudflare-workers",
    "deno",
    "edge"
  ],
  "type": "module",
  "main": "./pkg/ojs_wasm_sdk.js",
  "module": "./pkg/ojs_wasm_sdk.js",
  "types": "./pkg/ojs_wasm_sdk.d.ts",
  "exports": {
    ".": {
      "types": "./pkg/ojs_wasm_sdk.d.ts",
      "browser": "./pkg/ojs_wasm_sdk.js",
      "import": "./pkg/ojs_wasm_sdk.js",
      "default": "./pkg/ojs_wasm_sdk.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "pkg/ojs_wasm_sdk_bg.wasm",
    "pkg/ojs_wasm_sdk_bg.wasm.d.ts",
    "pkg/ojs_wasm_sdk.js",
    "pkg/ojs_wasm_sdk.d.ts",
    "pkg/snippets/**/*.js",
    "pkg/package.json",
    "README.md",
    "LICENSE"
  ],
  "sideEffects": [
    "./pkg/snippets/**"
  ],
  "scripts": {
    "build": "rm -rf pkg && wasm-pack build --target web --release --out-dir pkg && node scripts/prepare-package.mjs pkg",
    "build:bundler": "rm -rf pkg-bundler && wasm-pack build --target bundler --release --out-dir pkg-bundler && node scripts/prepare-package.mjs pkg-bundler",
    "build:nodejs": "rm -rf pkg-node && wasm-pack build --target nodejs --release --out-dir pkg-node && node scripts/prepare-package.mjs pkg-node",
    "test:features": "node scripts/check-feature-exports.mjs",
    "test:package": "node scripts/package-smoke.mjs",
    "clean": "rm -rf pkg pkg-bundler pkg-node .feature-matrix .npm-package-smoke-* release-artifacts",
    "prepublishOnly": "npm run build && npm run test:package"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=18"
  }
}
